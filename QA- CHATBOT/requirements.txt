# =============================================================
# requirements.txt  —  CLEAN VERSION FOR DEPLOYMENT
# =============================================================
#
# HOW TO INSTALL:
#   pip install -r requirements.txt
#
# WHY WE REWROTE THIS:
#   Your original requirements.txt was auto-generated by pip freeze from your venv.
#   It contained 80+ packages including Windows-only ones (.pyd files).
#   Render (Linux server) will fail trying to install Windows packages.
#
#   This version lists only what your project actually needs.
#   The others install automatically as dependencies.
#

# --- Core RAG & LangChain ---
langchain==1.2.15
langchain-community==0.4.1
langchain-core==1.2.31
langchain-groq==1.1.2
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2

# --- Vector store ---
faiss-cpu==1.13.2

# --- Embeddings & Reranker ---
sentence-transformers==5.4.1
transformers==5.5.4
torch==2.11.0

# --- BM25 lexical search ---
rank-bm25==0.2.2

# --- PDF reading ---
PyMuPDF==1.27.2
pypdf==6.10.2

# --- API server ---
fastapi==0.136.1
uvicorn==0.47.0
python-multipart==0.0.29

# --- Rate limiting (NEW in Phase 1) ---
slowapi==0.1.9

# --- Environment variables ---
python-dotenv==1.2.2

# --- Groq LLM client ---
groq==0.37.1

# --- Utilities ---
tqdm==4.67.3
numpy==2.2.6